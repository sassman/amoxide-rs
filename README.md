# amoxide (am) - the alias manager

> Coming soon — manage your shell aliases globally, per-profile, or per-project.

```
cargo install amoxide          # the `am` binary
cargo install amoxide-tui      # the `am-tui` interactive interface
```

![am tui](assets/am-tui-2.png)
