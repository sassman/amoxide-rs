//! Top-level entry point for directory-organised integration test modules.
//! Each `mod` line below maps to a folder under `tests/` whose `mod.rs`
//! declares the per-target submodules cargo will discover.

mod setup;
