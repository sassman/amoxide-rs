{"toml":"[[profiles]]\nname = \"rust\"\n\n[profiles.aliases]\nf = \"cargo fmt\"\nl = \"cargo clippy --locked --all-targets -- -D warnings\"\nt = \"cargo test --all-features\"\n","readmeHtml":"<h1>Rust Essentials</h1>\n<p>Three aliases I use on every Rust project — format, lint, test. Nothing fancy, just less typing.</p>\n<h2>Aliases</h2>\n<table>\n<thead>\n<tr>\n<th>Alias</th>\n<th>Expands to</th>\n<th>When I use it</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>f</code></td>\n<td><code>cargo fmt</code></td>\n<td>Format before committing</td>\n</tr>\n<tr>\n<td><code>l</code></td>\n<td><code>cargo clippy --locked --all-targets -- -D warnings</code></td>\n<td>Lint with strict warnings</td>\n</tr>\n<tr>\n<td><code>t</code></td>\n<td><code>cargo test --all-features</code></td>\n<td>Run the full test suite</td>\n</tr>\n</tbody>\n</table>\n<h2>Typical workflow</h2>\n<pre><code class=\"language-bash\">f           # format\nl           # lint — fix anything clippy complains about\nt           # test — make sure nothing broke\n</code></pre>\n<h2>Notes</h2>\n<ul>\n<li><code>l</code> uses <code>--locked</code> to ensure the lockfile is respected and <code>-D warnings</code> to treat warnings as errors</li>\n<li><code>t</code> uses <code>--all-features</code> to test everything, not just the default feature set</li>\n</ul>\n"}