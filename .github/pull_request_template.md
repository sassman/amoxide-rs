<!--
This body becomes the squash-merge commit body, which becomes the release-notes
paragraph for this change. Write it for users reading the next release page.

Trivial PRs (typo fix, lint): keep only the `## Why` section, one sentence.
-->

## Why

<!-- One paragraph: what problem this solves and why now. -->

## What changed

<!-- User-visible changes as bullets or short paragraphs. Skip implementation noise. -->

## Migration

<!-- Required when the PR title uses `!` (feat!, fix!, refactor!).
     Before/after snippets, renamed flags, removed APIs.
     Delete this section otherwise. -->
